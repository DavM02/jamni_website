export default function ArticleTitle({ title }) {
    return (
        <div style={{'--area': 'a'}} >
            <span className="xsmall-text text-main up-case">{title}</span>
        </div>
    );
}
